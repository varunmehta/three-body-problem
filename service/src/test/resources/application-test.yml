## Derive main properties from application.properties,
## override only the test related properties.

# Datasource config
spring:
  datasource:
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    url: "jdbc:tc:postgresql:10.11:///postgres_db"
    username: postgres
    password: postgres